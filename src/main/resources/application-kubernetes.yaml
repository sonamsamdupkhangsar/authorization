# this profile is activated by kubernetes dependencies
spring:
  cloud:
    kubernetes:
      enabled: true
      discovery:
        namespaces:
           - main
        include-not-ready-addresses: true
eureka:
  client:
      enabled: false

allowedOrigins: ${ALLOWED_ORIGINS}  #localhost
authzmanager: https://platform.admin.openissuer.com
